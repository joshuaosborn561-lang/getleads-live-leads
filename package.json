{
  "name": "sg-engager-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SalesGlider engager pipeline worker: inbox → verify → leads_staging → Smartlead",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "sweep": "node src/index.js --once",
    "test": "node --test --test-concurrency=1 tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4"
  }
}
