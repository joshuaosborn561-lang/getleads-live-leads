{
  "name": "sg-engager-pipeline",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SalesGlider engager pipeline: getleads pull → gates → resolve → webhook → verify → Smartlead",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/index.js",
    "once": "node src/index.js --once",
    "test": "node --test --test-concurrency=1 tests/*.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.57.4",
    "apify-client": "^2.25.0"
  }
}
